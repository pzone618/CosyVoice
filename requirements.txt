--extra-index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/ # https://github.com/microsoft/onnxruntime/issues/21684
conformer==0.3.2
deepspeed==0.15.1; sys_platform == 'linux'
diffusers==0.29.0
fastapi==0.115.6
fastapi-cli==0.0.4
gdown==5.1.0
gradio
grpcio
grpcio-tools
hydra-core==1.3.2
HyperPyYAML==1.2.2
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
matplotlib
modelscope==1.20.0
networkx==3.1
numpy
omegaconf==2.3.0
onnx
onnxruntime-gpu==1.18.0; sys_platform == 'linux'
onnxruntime; sys_platform == 'darwin' or sys_platform == 'win32'
openai-whisper
protobuf
pyarrow==18.1.0
pydantic
pyworld
rich==13.7.1
soundfile==0.12.1
tensorboard==2.14.0
tensorrt-cu12==10.13.3.9; sys_platform == 'linux'
tensorrt-cu12-bindings==10.13.3.9; sys_platform == 'linux'
tensorrt-cu12-libs==10.13.3.9; sys_platform == 'linux'
torch
torchaudio
transformers==4.51.3
x-transformers==2.11.24
uvicorn==0.30.0
wetext==0.0.4
wget==3.2

# Python 3.13 Compatibility:
# The following packages have been unpinned from specific versions because
# the original pinned versions (e.g. torch 2.3.1) do not provide pre-built wheels
# for Python 3.13 on macOS ARM64 as of late 2025.
# Using the latest stable versions solves the build issues.
# - grpcio, grpcio-tools, matplotlib, numpy, onnx, openai-whisper
# - protobuf, pydantic, pyworld, torch, torchaudio
